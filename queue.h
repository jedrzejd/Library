#include <stdio.h>
#include <stdlib.h>
#include "book.h"
struct item{
    book val;
    struct Queue *next;
}queue;

void Add(book val, val_arr *item){
    while (item->next != NULL)
        item = item->next;

    // queue *pom  
    
}